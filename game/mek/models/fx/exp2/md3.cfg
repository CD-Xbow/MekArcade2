//Explosion Experiments with the rocket
shader 0 crosshader [
    @vpstart
    PARAM seconds = program.env[5];

    MAD result.texcoord[0], seconds, -0.2, vertex.texcoord[0];
    MAD result.texcoord[1], seconds, { 0.9, 0.9, 0, 0 }, vertex.texcoord[0];

    MOV result.color, vertex.color;

    @fogcoord
    END
] [
    @fpstart
    OPTION ARB_fog_linear;
    TEMP dtc, diffuse;

    TEX dtc, fragment.texcoord[0], texture[0], 2D;
    MAD dtc.xy, dtc.xzxz, 0.1, fragment.texcoord[1];
    TEX diffuse, dtc, texture[0], 2D;

    MOV result.color.rgb, diffuse;
    MUL result.color.a, diffuse.a, fragment.color.a;

    END
]

md3load tris.md3

//md3skin mesh skin.png masks.jpg .6 .1
md3bumpmap mesh misc.png
md3skin mesh misc.png  //was misc 
md3shader mesh crosshader
md3alphatest mesh 0

mdlenvmap 0 0 skyboxes/clouds

mdlcullface 0
mdlspec 100

//md3anim mapmodel	0   60  15
md3anim "mapmodel" 0 1   // only used when not a trigger
md3anim "trigger on" 0 16 16      //  last anim is on position
md3anim "trigger off" 16 1 1    // last anim is off position

//md3skin mesh skin.png masks.jpg .6 .1
//md3bumpmap mesh normal.jpg  //ridges
//md3pecmap mesh misc.png
//md3shader mesh crosshader
//md3alphatest Cross 0

